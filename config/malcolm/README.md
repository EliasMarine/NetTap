# Malcolm Configuration Overlays

This directory contains NetTap-specific configuration overrides for the Malcolm stack.

Place Malcolm config files here that differ from Malcolm defaults. These are mounted
into the Malcolm containers as read-only volumes.
