import { describe, it, expect } from 'vitest';

describe('Homepage Server Load', () => {
	it('should have server load function defined', () => {
		// +page.server.ts handles auth redirect logic
		expect(true).toBe(true);
	});
});
